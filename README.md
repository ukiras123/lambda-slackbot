# slack-bot-lambda
S37 Slack Bot: after deployment it creates a API Gateway and Lambda Function in AWS. It has the with following features:

- Send OQ Status


### How to deploy

`npm install`

`npm run build`

`npm run deploy`


